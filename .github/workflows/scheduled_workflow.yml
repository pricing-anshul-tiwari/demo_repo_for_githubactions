name: 'Scheduled workflow'
on:
  schedule:
    - cron: '*/2 * * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduled workflow
        run: |
          echo "Scheduled workflow"